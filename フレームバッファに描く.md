## フレームバッファとは
コンピュータのディスプレイに画像（広義ではすべてが画像。ピクセルのまとまり）を表示するために1画面分（文字通り１画面。ディスプレイが１枚なら画面は１つ）の画像データを一時的に保存しておくメモリ領域。
ディスプレイに次に何を表示するかを指示するための下書き用紙のようなもの。ホワイトボードとも例えられる。

### フレームバッファに描くために
UEFIとやり取りして、フレームバッファのアドレスを知ることができる。
アドレスを知ることができれば、そこに書き込めばフレームバッファの内容を変更できて、表示内容を調節できる。

### EFI Graphics Output Protocol(GOP)
フレームバッファ関連の情報を扱うプロトコル。
各種のプロトコルにはGUID(Global Unique Identifier)が割り当てられている。
GUIDはコンピュータ内で一意の識別子。世界で一つになる。
このGUIDを`locate_protocol()`関数に渡すと、対応するプロトコルへのポインタを取得できる。
`locate_protocol()`関数のアドレスは、**EFI System Table**という構造体の中にある。
**EFI System Table**を指すポインタは、EFIアプリケーションの第2引数。

つまり
* `efi_main`の第二引数にEFI System Tableのポインタがある。
* その中からlocate_protocol()関数を取得。
* この関数にGOPに対応するGUIDがある。
* GUIDをlocate_protocol()に渡せば、フレームバッファを扱う関数がゲットできる
* あとは思い思いに描く

